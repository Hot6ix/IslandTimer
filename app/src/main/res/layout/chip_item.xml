<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android" android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="2dp">

    <TextView
        android:id="@+id/chip_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/padding5"
        android:paddingBottom="@dimen/padding5"
        android:paddingStart="@dimen/padding8"
        android:paddingEnd="@dimen/padding8"
        android:textSize="@dimen/text_summary"
        android:textColor="@android:color/white"
        android:background="@drawable/chip_item" />

</androidx.constraintlayout.widget.ConstraintLayout>